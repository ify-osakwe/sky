# ===== Production Profile Overrides =====
# Only includes settings that differ from application.properties.
# Everything else (datasource, flyway, cache, swagger, etc.) is inherited.

# --- Logging ---
logging.level.github.com.ifyosakwe.sky=INFO
logging.level.org.springframework.web=ERROR

# --- Scheduler ---
sky.scheduler.stale-data-days=15
